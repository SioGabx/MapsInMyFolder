<UserControl
    x:Class="MapsInMyFolder.UserControls.SearchLayer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:commun="clr-namespace:MapsInMyFolder.Commun"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <TextBox
            x:Name="layer_searchbar"
            HorizontalAlignment="Stretch"
            Cursor="IBeam"
            GotFocus="Layer_searchbar_GotFocus"
            KeyDown="Layer_searchbar_KeyDown"
            LostFocus="Layer_searchbar_LostFocus"
            Style="{DynamicResource TextBoxCleanStyle_16}"
            Text="{Binding Source={x:Static commun:Languages.Current}, Mode=OneWay, Path=[searchLayerPlaceholder]}"
            TextChanged="Layer_searchbar_TextChanged" />
    </Grid>
</UserControl>
